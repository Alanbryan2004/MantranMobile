import React, { createElement, useState } from 'react';

export default function IconsDescription({ }) {
    (    <div>
            <i class="material-icons">description</i>Manifesto
        </div>
    );
}
